# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Leaflet map renderer shipped (spec 07 shipped 2026-05-20 on `feat/07-leaflet-map-renderer`). The visual deliverable the assessment grades against is live on `/trips/:id`. Next: spec 08 §395.8 Daily Log SVG renderer (FE-only; consumes the same plan endpoint).

## Current Goal

- Author `context/specs/08-eld-log-svg-renderer.md` — render one §395.8 24-hour grid SVG per `LogDay` (from the plan envelope's `events` + `days` arrays). The grid + duty-status lines + per-status totals + Remarks city/state at each duty change. Multiple day-sheets stacked vertically; PDF export queues for spec 09.

## Completed

- 2026-05-19 — `context/specs/01-shared-ui-foundation.md` authored (`packages/ui` shadcn primitives + `BrandMark` + `SpotterLoader` + `ThemeProvider` / `useTheme` / `ThemeToggle` + Vitest harness).
- 2026-05-19 — `context/specs/02-clerk-auth-screens.md` authored (`apps/web-auth` custom Clerk flows with floating video panel, OAuth, inline email-code verification, bot protection, forgot-password, theme toggle, accessibility floor).
- 2026-05-19 — `feat/01-shared-ui-foundation` implemented (10 shadcn primitives, inline-JSX `BrandMark` + animated `SpotterLoader`, `ThemeProvider` / `useTheme` / `ThemeToggle` colocated under `components/theme/`, Vitest 4 + RTL 16 harness with 21 colocated tests, exports override for the .ts hook, sonner wired to the new theme primitive).
- 2026-05-19 — `feat/02-clerk-auth-screens` implemented. Custom Clerk Core 3 / Future API flows on `apps/web-auth`: sign-in (Google OAuth + password), sign-up (Google OAuth + password + Turnstile + inline email-code verification with paste-distribute OTP cells + 30s resend cooldown), forgot-password (email → code → new password). Brand-themed layout with mirrored auth video (desktop/mobile/poster, ≤4.8 MB) + reduced-motion fallback. WCAG 2.2 AA across all flows (skip link, `Field`/`FieldLabel`/`FieldError`, `aria-pressed` toggles, `role="alert"` server-error banners, OTP `role="group"`). Round-2 sub-agent review (commit `adbd953`) tightened the contract: `aria-describedby` wired input→FieldError on every form, focus management on forgot-password reset phase, "Password visibility" constant noun for the show/hide toggle, FieldError switched to `aria-live="polite"`, redundant `aria-live="assertive"` removed where `role="alert"` already implies it, "Use a different email" resets RHF state, sign-in focus returns to password after server error, WCAG 2.5.8 touch-target bump on Resend / Use-a-different-email, `console.warn` removed from forgot-password to close the OWASP enumeration leak. Full Playwright MCP E2E matrix exercised in-session and recorded in the PR body. 76 colocated Vitest tests + 22 test files. Clerk dev instance reconfigured via CLI (Google OAuth enabled, HIBP on, min_length 10, complexity rules, min zxcvbn 3, email_code verification, Turnstile bot protection).
- 2026-05-19 — `chore/04-remove-legacy-env-examples` implemented. Renamed `apps/web-app/.env.example` → `apps/web-app/.env.local.example` and `apps/web-api/.env.example` → `apps/web-api/.env.local.example`, updated the `cp` snippet in `apps/web-app/README.md`, updated the comment header in `apps/web-api/.env.local.example` (was `Copy to .env`, now `Copy to .env.local`), and updated `context/code-standards.md` line 90 (Django settings reference). Closes the `.env.local.example` convention rollout across the monorepo.
- 2026-05-19 — `context/specs/03-trip-input-foundation.md` authored. Single vertical slice that lands the `apps/web-app` shell (single-rail `<Sidebar collapsible="icon">` inspired by shadcn `sidebar-09` but reduced to one rail until a list-pane consumer exists; custom `NavUser` built from `Avatar` + `DropdownMenu` + `useUser()` + `useAuth()` — explicitly **not** Clerk's `<UserButton />` so the brand owns its identity surface and the dropdown can host `ThemeToggle`), the assessment-mandatory trip-input form (three address `Combobox` fields wired to debounced Pelias autocomplete + `Slider` + numeric cycle-hours mirror + visible remaining-cycle `Progress` band, RHF + zod validation, discriminated-union address state that rejects free-text at submit-time), and the BE half (typed `web_api/integrations/openrouteservice.py` `requests` client clamped to `boundary.country=US` + `size ≤ 10`, two `IsAuthenticated` DRF views at `/api/geocode/{autocomplete,search}/`, stub `Trip` model + `POST /api/trips/` create echo + ownership-gated retrieve). Spec calls out the FE+BE boundary deviation explicitly (justified as one user-value slice with the BE limited to thin proxies + a stub) and reshuffles the queue: ORS Directions becomes spec 04, HOS planner foundation becomes spec 05.
- 2026-05-20 — `feat/04-ors-directions-trip-pipeline` implemented. BE: extended `web_api/integrations/openrouteservice.py` with `directions_hgv(coordinates)` POSTing to `/v2/directions/driving-hgv/geojson`. Three frozen-slots dataclasses (`DirectionsSegment`, `DirectionsSummary`, `DirectionsResult`) lock the response shape; `OrsRateLimitError` gains a `*, window: Literal["per-minute", "daily"]` kwarg so the view layer can distinguish quotas. POST added to `Retry.allowed_methods` (read-only-effectively assumption documented in module docstring). Status mapping: 429→per-minute, 403+"quota"→daily, 403/401/400→OrsRequestError, 5xx→OrsUpstreamError. `Trip` model gains `TripStatus(TextChoices)` enum (PLANNING/PLANNED/FAILED — "pending" dropped) + four nullable route fields. New `TripRouteCache` table keyed by SHA256 of `"v1|driving-hgv|recommended|mi|{lon:.5f},{lat:.5f}|…"`. New `web_api/apps/trips/services.py::plan_trip` is the pipeline entry point: atomic Trip insert in PLANNING → cache lookup → ORS call → cache write (`update_or_create` handles concurrent races via Django's internal SELECT FOR UPDATE + IntegrityError retry) → Trip update to PLANNED, or `_mark_failed(route_error_code)` to FAILED on any `OrsError`. `TripCreateView` always returns 201 with the discriminated row (decision 14). New `web_api/throttling.py::PerUserScopedThrottle` subclasses `ScopedRateThrottle` and keys on `request.user_id` instead of `request.user` (which is `AnonymousUser` under JWT-only auth); rates: `geocode_autocomplete=60/min`, `geocode_search=20/min`, `trip_create=30/hour`. Migration 0002 ports any leftover `"pending"` row to `"planning"` via `RunPython` placed AFTER the `AddField` ops (so reverse RunPython filters `route_polyline__isnull=True` while the column still exists). FE: new `features/trip-planner/schemas/trip-response.ts` zod `discriminatedUnion("status", [planned, planning, failed])`; `useTripById` and `usePlanTrip` parse responses through it; `usePlanTrip` widens return type from a 3-field echo to the full `TripResponse`. New `features/trip-planner/components/route-summary.tsx` renders a single `<Card>` + `<CardContent>` + one `<dl>` with two `<div role="group">` blocks (hero distance/duration + per-leg "Current → Pickup" rows) on PLANNED, `<SpotterLoader size="lg" />` on PLANNING, `<Empty>` with copy keyed by `route_error_code` + "Plan a new trip" link on FAILED. No `<CardHeader>`, no duplicate badge, no retry button, no map placeholder, no icons on labels — all 10 spec anti-patterns enforced. `TripDetailPanel` restructured into two `<SidebarGroup>` blocks (Route + Inputs) with the status badge appearing exactly once on this surface (decision 22). `/trips/:id` placeholder deleted; route summary IS the spec-04 deliverable. New utils `format-duration.ts` (`s → "Xh Ym"`) and `format-distance.ts` (`mi → "X.X mi"`). MSW handlers updated to emit `status: "planned"` by default + named exports `mockTripPlanned/Planning/Failed`. Test totals: 69 BE (10 directions client + 9 trip pipeline + 5 throttle + 11 trips views + 11 geocoding views + 10 ORS Pelias client + 3 smoke + 7 auth + 3 trips views legacy) and 43 FE (6 route summary + 10 utils + 4 trip detail panel + 3 trips detail route + 12 trip input schema + 3 cycle hours + 2 trip input form + 3 address field). Web-app build 760.77 KB raw / 232.27 KB gzip (+11 KB raw / +3 KB gzip vs spec 03 baseline); confirmed zero leaflet leakage. Five sub-agent reviews ran against the diff (code-reviewer / architect-review / security-auditor / ui-visual-validator / performance-engineer); the only blocking finding was the ui-visual-validator catching a weak focus indicator on the "Plan a new trip" link — fixed in commit `8cb241a` with the standard `focus-visible:ring-*` token pattern from `context/ui-context.md`. Architect-review against the spec text (pre-implementation) caught two small spec drifts that were corrected in commit `62de3f4`: the RunPython placement (must be AFTER AddField for reverse symmetry) and an explicit 401 → `OrsRequestError` branch in decision 7.

- 2026-05-19 — `feat/03-trip-input-foundation` implemented. BE: typed `requests`-based ORS Pelias client (`web_api/integrations/openrouteservice.py`) with `PeliasFeature` dataclass + `OrsRateLimitError` / `OrsRequestError` / `OrsUpstreamError` mapped to 429 / 400 / 502 at the view boundary. `web_api/apps/geocoding/` (`/api/geocode/{autocomplete,search}/`, `IsAuthenticated`, `text` 1..200, `size` clamped, `focus_lat`/`focus_lon` paired). `web_api/apps/trips/` (stub `Trip` model UUID PK + Clerk `user_id` + three flat address triples + `cycle_hours_used` Decimal(3,1) + `status` + `(user_id, -created_at)` index; create stamps `user_id` server-side; retrieve enforces ownership → 403). `OPENROUTESERVICE_API_KEY` bumped to `SecretStr`. `web_api/settings/test.py` new (SQLite for hermetic pytest runs). FE: `apps/web-app` `app-shell/{app-shell-layout, app-sidebar, nav-user}.tsx` (single icon-rail, custom NavUser with ThemeToggle + Sign out), router refactored to nested layout under `RequireAuth`, new routes `IndexRedirect` / `TripsNewRoute` / `TripsDetailRoute`. Bulletproof `features/trip-planner/` (schemas + TanStack hooks + RHF form, debounced Pelias autocomplete via Combobox, Slider + numeric mirror + Progress band). 12 shadcn primitives + `Empty` installed in `packages/ui`. 22 colocated Vitest specs + MSW handlers + 42 pytest cases. Playwright MCP browser smoke walked the golden path desktop+mobile (Richmond → Fredericksburg → Newark, 35.0 cycle hours, real ORS Pelias responses, real Postgres write). Web-app build 747 KB raw / 227 KB gzip with the documented chunk warning.

- 2026-05-20 — `feat/06-hos-persistence-start-at-plan-endpoint` implemented (4 commits on the branch). Three new Django models — `TripStop` (UUID + FK→Trip CASCADE + `kind` in 6-member StopKind + `sequence` + `polyline_index` + Decimal(9,6) lat/lon + `scheduled_at` + `duration_s`), `LogEvent` (UUID + FK→Trip CASCADE + `sequence` + `status` from DutyStatusChoices + `start` + `duration_s` + `location` + `note`; two indexes `(trip, sequence)` and `(trip, start)`), `LogDay` (per-home-terminal-local date rollup with four per-status `*_s` totals + Decimal(7,1) `total_miles`). `Trip.start_at` added; migration 0004 lands `AddField(null=True)` → RunPython `Coalesce("created_at", Now())` backfill → `AlterField(null=False)` → CreateModel × 3 (defensive Coalesce is code-reviewer C2 fix — guards against rows inserted via raw SQL bypassing `auto_now_add`). New `web_api/apps/trips/hos_adapter.py::materialize_plan(trip)` is the SINGLE one-way bridge between the pure-Python planner and the Django ORM (invariant #1): imports `web_api.hos` symbols + `web_api.hos.fueling.fuel_stop_indices`; `web_api.hos.*` does NOT import the adapter. Midnight-crossing events persist as one `LogEvent` row (invariant #2) but split their seconds + miles across `LogDay` rows by home-terminal-local date via `_attribute_to_days`. Miles attribution uses `total_trip_miles / total_driving_s` (per-fragment proportional, no magic constant), so daily totals sum to the ORS summary up to a 0.1-mi rounding delta. `services.plan_trip` wraps `Trip.objects.create()` AND `materialize_plan(trip)` in ONE `transaction.atomic` block — any failure rolls back the entire write. `TripCreateView` maps planner `ValueError` to HTTP 422 (distinct from ORS 4xx/5xx). New `TripPlanView(RetrieveAPIView)` at `GET /api/trips/<uuid:id>/plan/`: ownership-gated via `get_queryset` filtering on `request.user_id` (foreign trips surface as 404 — no oracle), `prefetch_related("stops", "log_events", "log_days")` so the retrieve runs in 4 queries (1 Trip + 3 prefetch batches — Django doesn't batch multiple prefetch targets into one SQL statement), throttled at `trip_plan_retrieve = 120/min` via the existing `PerUserScopedThrottle`. `TripPlanSerializer.to_representation` composes the envelope `{trip_id, start_at, home_terminal_tz, stops, events, days}` reading prefetched relations; lat/lon as Decimal strings (DRF default), timestamps ISO 8601. `_validate_start_at_not_past` is a module-scope CALLABLE (architect-review M1) that reads `timezone.now()` fresh each request — `MinValueValidator(timezone.now() - …)` would have frozen the cutoff at worker boot. `_request_user_id` raises a 500 `_MissingUserIdentity(APIException)` (security-auditor M-1, not 403 PermissionDenied) since a JWT-authenticated request without `user_id` is a server invariant violation, not a client problem. `_PLANNER_FAILED` user copy doesn't name internal subsystems (security-auditor M-3). `GZipMiddleware` added to MIDDLEWARE between SecurityMiddleware and WhiteNoiseMiddleware (performance-engineer M1) so the JSON envelope ships ~5-7× smaller. FE adds `<StartAtField>` (datetime-local picker; no `min` attribute — zod refine is the past-time source of truth, typescript-pro M1) mounted below `<CycleHoursField>` in the trip form, plus a "Departs" `<dt>`/`<dd>` row in `TripDetailPanel`'s Route SidebarGroup formatted via module-cached `Intl.DateTimeFormat` in `America/New_York`. New `round-time.ts` + `format-start-at.ts` pure utils + colocated tests. Zod uses `z.iso.datetime({ offset: true })` (v4 API). `useTripPlan` TanStack hook deliberately deferred to spec 07 with its first consumer (per spec decision 15) — no unused exports. New cross-module contract test `tests/hos/test_note_contract.py` pins the planner's `LogEvent.note` strings to the adapter's `_NOTE_TO_KIND` table (code-reviewer C1, architect-review M1): asserts pairwise non-overlap, classification for each canonical note, and named-constant ↔ dict-key parity. Boundary test in `tests/hos/test_boundary.py` extended with `test_duty_status_parity_with_django_choices` so a planner-side enum addition fails CI before a divergent migration lands. Test totals: 172 BE (142 prior + 30 new across `test_hos_adapter.py` 8 + `test_models_plan.py` 7 + `test_trip_plan_view.py` 7 + `test_note_contract.py` 3 + 5 extensions to existing files) + 53 FE (35 prior + 18 across `start-at-field` 4 + `round-time` 6 + `format-start-at` 4 + extensions). Browser smoke walked end-to-end on real ORS + real Postgres: Richmond → Fredericksburg → Newark golden produced 3 TripStops (pickup + auto-inserted §395.3(a)(3)(ii) break + dropoff) + 6 LogEvents + 2 LogDays (208.0 + 130.5 = 338.5 mi total — midnight split correct), `curl /api/trips/<id>/plan/` returned the spec-compliant envelope, past-time submission triggered `data-invalid="true"` + zod refinement copy, bogus + foreign UUIDs both 404. Six sub-agent reviews against the diff (architect-review / code-reviewer / security-auditor / django-pro / typescript-pro / ui-visual-validator / performance-engineer); resolved feedback landed in commit `3cfd170`: planner copy + 403→500 + GZip middleware + Coalesce backfill + test-name fix + throttle test dict + planner-note contract test + drop FE `min` attribute + drop redundant `useController` defaultValue.

- 2026-05-20 — `feat/07-leaflet-map-renderer` implemented. FE-only unit (zero `apps/web-api/**` diff; invariants #1 / #4 hold). Dependencies: `leaflet@1.9.4`, `react-leaflet@5.0.0` (peer-deps satisfied for React 19.2.6), `@types/leaflet@1.9.21` (dev). New `/trips/:id` main area: lazy-loaded `<TripMap />` (default export) renders `<MapContainer>` + OSM `<TileLayer>` + brand-themed `<RoutePolyline>` (teal-600, 4px) + per-`StopKind` `<StopMarker>` (`L.divIcon` with inline SVG colored via `.trip-marker__icon--{kind}` CSS classes — CSP-safe, zero inline `style`) + `<FitToRoute>` (single fire per non-empty mount via `useRef<boolean>` sentinel) + `<RecenterControl>` (visible Compass button paired with `R` keystroke; both share one `recenter` `useCallback`). Marker popups (`.leaflet-popup-themed`) render shadcn `Badge` (kind label) + §395 reason line for planner-inserted stops (break/sleeper/restart, no reason for pickup/dropoff/fuel) + scheduled time (module-cached `Intl.DateTimeFormat` keyed on `plan.home_terminal_tz`) + location label or `formatLatLon` fallback + duration. `RouteSummary` repurposed to panel-mode (one `<dl>` with Total + per-leg + Departs rows; the `<Card>` wrapper removed); mounted inside `TripDetailPanel`'s Route `SidebarGroup` replacing the hand-built `<dl>`. `useTripPlan(tripId)` TanStack hook (spec-06 decision 15 carry-over) lands in `features/trip-planner/api/trip-plan.ts` with `["trip", tripId, "plan"]` queryKey, `staleTime: 5*60_000`, `retry: false`, `refetchOnWindowFocus: false`. Zod schema `tripPlanSchema` mirrors the spec-06 envelope (`{trip_id, start_at, home_terminal_tz, stops, events, days}`); `z.coerce.number()` for DRF decimal-strings, `z.iso.datetime({ offset: true })` for ISO 8601. Distinct trip-404 vs plan-404 empty states per spec decision 16 (`Trip not found` with new-trip link vs `Trip data missing` without). MSW `mockTripPlan` + `samplePlanShape` helper added to default handlers. `<MapContainer>` has no JSX `aria-label` (react-leaflet 5 drops arbitrary props); the label is set post-mount via `mapRef.current.getContainer().setAttribute('aria-label', …)`. R-key handler uses `container.contains(document.activeElement)` so it fires when focus is on a marker / popup / attribution / recenter button — strict identity would have broken once the user tabbed off the container. Stable `positionsRef` keeps the keydown listener from re-attaching on tile-load cascades. Module-level `ICON_REGISTRY` reuses six `L.divIcon` instances across every render instead of allocating per-marker per-render. **Bundle architecture**: lazy-loaded chunk via `React.lazy(() => import("./trip-map"))` — Vite/Rolldown's natural split places leaflet + react-leaflet + @react-leaflet/core + leaflet.css into `trip-map-*.js` (160.25 KB raw / 47.73 KB gzip) + `trip-map-*.css` (15.09 KB raw / 6.36 KB gzip). Entry chunk 758.04 KB raw / 231.80 KB gzip (+0.81 KB raw vs spec-04 baseline, well under the +5 KB ceiling). `dist/index.html` has zero leaflet preload tags. An explicit `manualChunks` carve-out was tried but caused Rolldown to hoist shared React deps into the leaflet chunk via cross-chunk imports — the natural lazy-import split is correct. **CSS additions**: `packages/ui/src/styles/globals.css` gains one block (`.leaflet-popup-themed` + `.trip-marker*` + `prefers-reduced-motion`-gated marker fade-in + tile transition); zero token additions, zero `@theme inline` edits. `.trip-marker__icon` carries a white `drop-shadow` filter so markers stay visible against any OSM tile background (resolves ui-visual-validator C-1: fuel/dropoff/break would otherwise fail WCAG 1.4.11 against light gray tiles). Popup Badge override (`.leaflet-popup-themed [data-slot="badge"].trip-marker__icon--{kind}`) restores `color: var(--foreground)` so the badge label text isn't tinted to the marker color — keeps WCAG 1.4.3 4.5:1 ratio for all six kinds (resolves ui-visual-validator C-2). Zod parse failures now throw `ApiError` with `body.detail = "Plan response shape unexpected"` per spec decision 9 (resolves typescript-pro C2). **UX polish layer**: visible Recenter button (`lucide` `Locate` icon, top-right overlay) paired with `R` key — discoverable for non-keyboard drivers AND keyboard-reachable; map container `bg-background` so the tile-load moment doesn't flash white; 200ms / 80ms-delay marker fade-in + 150ms tile cross-fade (both `prefers-reduced-motion: no-preference`-gated); distinguishable marker shapes (triangle pickup, pin dropoff, fuel pump, mug break, bed sleeper, restart arrow) so colorblind users can identify by silhouette; §395 reason line in popups telling the driver WHY each planner-inserted stop exists. Test totals: 305 (172 BE unchanged + 112 web-app FE + 21 UI). New files: `features/trip-planner/api/trip-plan.{ts,test.tsx}`, `schemas/trip-plan.{ts,test.ts}`, `components/trip-map.{tsx,test.tsx}`, `components/map/{stop-type-colors.ts, stop-kind-labels.ts, marker-icons.{tsx,test.tsx}, route-polyline.{tsx,test.tsx}, stop-marker.{tsx,test.tsx}, marker-popup.tsx, fit-to-route.{tsx,test.tsx}, recenter-control.tsx}`, `utils/{format-lat-lon,keyboard}.{ts,test.ts}`. Modified: `route-summary.{tsx,test.tsx}`, `trip-detail-panel.tsx`, `trips-detail.{tsx,test.tsx}`, `testing/handlers.ts`, `vite.config.ts`, `packages/ui/src/styles/globals.css`. Four sub-agent reviews ran in parallel against the diff (code-reviewer / typescript-pro / ui-visual-validator / performance-engineer); five CRITICAL findings + several MAJORs resolved in one fix pass: the silently-dropped `aria-label` (typescript-pro C1), the zod→ApiError contract (typescript-pro C2), the marker contrast halo (ui-visual-validator C-1), the popup badge text contrast (ui-visual-validator C-2), the entry-chunk leaflet leak via manualChunks back-edge (performance-engineer CRITICAL). Browser smoke deferred to the user's local workstation (matches spec-06 precedent — requires Clerk + ORS secrets the agent doesn't have).

- 2026-05-20 — `feat/05-hos-planner-foundation` implemented. Pure-Python `apps/web-api/web_api/hos/` module ships `plan_logs(PlannerInputs) -> list[LogEvent]` honoring §395.3(a)(1) 11-hour drive, §395.3(a)(2) 14-hour window, §395.3(a)(3)(ii) 30-min break, §395.3(b) 70/8 cycle cap, §395.3(c)(1) 34-hour restart, and `docs/assesment.md:18` 1000-mile fueling. Six §395-cited rule predicates in `rules.py`; `state.py` PlannerState ladder with pure `advance()`; `fueling.py` haversine + threshold sweep with 5% ORS-summary sanity check; `planner.py` slices long-haul driving legs around the soonest constraint (deliberate deviation from spec's "one rule emit per leg" pseudocode — `assessment_break_only` golden requires 8h drive → 30-min break → 1h drive). `_smoke.py` is a boundary-exempt CLI sanity script (`uv run python -m web_api.hos._smoke`) that prints planned events for all 4 golden scenarios. Tests: 73 new (boundary, types, state, fueling, rules-with-≥3-cases-per-rule, four §395-cited goldens, planner-replay invariants). `test_boundary.py` AST-walks every `.py` under `web_api/hos/` against an 11-entry `ALLOWED_TOP_LEVEL` allowlist (mirrored verbatim in architecture.md#Invariants) AND asserts TYPE_CHECKING-only imports for `web_api.integrations.openrouteservice` data-contract types (architect-review m1). `test_cycle_cap_subsumes_window` locks the M2 invariant (no preceding 10h OFF_DUTY at restart boundary); `test_advance_sleeper_berth_equivalent_to_off_duty` locks decision 9 / m3. Four sub-agent reviews (architect-review, code-reviewer, python-pro, test-automator) ran against the diff; review feedback resulted in: stronger `_emit_static_leg` docstring (window enforcement is lazy on driving leg), planner.py + rules.py docstrings now honestly describe the dual surface (rules.py predicates vs. planner.py slicer share constants), Decimal-int comment in `_max_drive_chunk`, and a new `test_off_duty_window_fires_when_window_already_expired` boundary case. Tooling: `uv run ruff check`, `uv run ruff format --check`, `uv run mypy --strict`, `uv run pytest -q` all green; `pnpm exec turbo run lint typecheck test --affected` cached green (zero TS/JS surface touched). Full test count: 142 BE (69 prior + 73 new) + 35 FE.

## In Progress

- None.

## Next Up

1. `feat/08-eld-log-svg-renderer` — render §395.8 Daily Log SVGs (one per `LogDay` row in the plan envelope) inside `TripDetailPanel` or a new full-page tab. Four duty-status rows (Off Duty / Sleeper Berth / Driving / On Duty Not Driving), 24-hour grid, per-status totals column, Remarks column auto-filled with city/state at each duty change. Multi-day trips stack vertically with date headers. Closes the "Draw on the log and fill out the sheet" assessment requirement. PDF export queues for spec 09.
2. `feat/<NN>-planner-event-kind-field` — promote the planner's `LogEvent.note` prefix coupling into a structured `kind: StopEventKind | None` field on `web_api.hos.types.LogEvent` so the adapter's `_NOTE_TO_KIND` table goes away. Today the contract test in `tests/hos/test_note_contract.py` pins the strings; this would eliminate the runtime classification entirely. Code-reviewer C1 from the spec-06 review, deferred.
3. `feat/<NN>-apple-oauth` — wire Apple OAuth once the Apple Developer account is provisioned. Adds the Apple button to `oauth-button-group.tsx`, the `connection_oauth_apple` Clerk patch, and the Apple OAuth row to the E2E matrix.
4. `fix/<NN>-auth-route-titles` — set `document.title` per route in `apps/web-auth` so `/sign-up` and `/forgot-password` no longer inherit the static `<title>Sign in · Outbound Spotter</title>` from `index.html`. Surfaced during the spec-02 E2E sweep.
5. `perf/<NN>-bundle-split` — split `password-rules.ts` so `@zxcvbn-ts/language-*` ships only on `/sign-up` + `/forgot-password`. (Spec-07 already proved the leaflet lazy split works via `React.lazy` alone — no `manualChunks` needed.) Either self-host fonts or preload the Google Fonts stylesheet. Tracked from the spec-02 + spec-03 + spec-04 `performance-engineer` reviews.
6. `fix/<NN>-auth-a11y-residual` — remaining `ui-visual-validator` items deferred from spec 02: M-2 (persistent live-region container in `sign-up-form`), M-3 (`aria-live="polite"` announcement before code-sent transition), L-1 (decorative `<video aria-hidden>`), L-2 (`autocomplete="one-time-code"` only on OTP cell 1), L-3 (skip-link text). Also the architect L1 housekeeping (Google brand-hex literals consolidated into a `BRAND_COLORS` const) and the `signIn.resetPasswordEmailCode.sendCode()` switch for `onResend` flagged in the round-2 code-reviewer pass.
7. `chore/<NN>-redis-throttle-cache` — wire Django's `CACHES["default"]` to Redis (Fly.io Upstash add-on) before multi-worker deployment, so the per-user throttle counters survive across gunicorn workers. Surfaced in the spec-04 `performance-engineer` review (MIN4); not blocking the assessment review (single-process).
8. `chore/<NN>-trip-response-schema-trim` — narrow the exports in `apps/web-app/src/features/trip-planner/schemas/trip-response.ts` so only the externally-consumed schemas (`tripResponseSchema`) and type aliases (`TripResponse`, `RouteErrorCode`, `TripPlanned`, `TripPlanning`, `TripFailed`) are exported. Per-branch schemas and intermediate type aliases (`RouteSegment`, `RouteSummary`) currently leak. Surfaced in the spec-04 `code-reviewer` review (MIN1).

## Open Questions

- (a) shadcn `sidebar` export-pattern drift — resolved during spec-03 implementation: the `./components/*` → `./src/components/*.tsx` pattern resolved every new primitive cleanly. The bundled `use-mobile.ts` hook landed at `packages/ui/src/hooks/`; the eslint override was extended to cover it. (b) Pelias features without `lat`/`lon` — not hit during live testing; the client raises `OrsUpstreamError` on malformed features, but a `/api/geocode/reverse/` endpoint + a `geocode_reverse(lat, lon)` callsite remains a known-unknown for the geolocation feature when it ships. (c) cmdk + Radix jsdom shims — added preemptively to `apps/web-app/src/testing/setup.ts` (IntersectionObserver, ResizeObserver, matchMedia, hasPointerCapture, scrollIntoView). All three resolved.

## Architecture Decisions

- 2026-05-20 — **TripPlanView retrieve runs in 4 SQL queries, not 2.** Spec 06 verification line 601 was over-optimistic about `prefetch_related` batching: Django runs ONE additional SELECT per prefetched relation (stops, log_events, log_days), not one combined batch. The retrieve is therefore `1 Trip lookup + 3 prefetch batches = 4 queries`. Test renamed `test_plan_runs_in_four_queries_via_prefetch`; serializer + view docstrings updated. Collapsing to ≤ 2 queries would require Postgres `array_agg` + JSON building in raw SQL — over-engineering for v1.
- 2026-05-20 — **`_request_user_id` raises a 500 (`_MissingUserIdentity` APIException), not 403 (`PermissionDenied`).** A JWT-authenticated request that passes `IsAuthenticated` but doesn't carry a Clerk `sub` is a SERVER invariant violation (misconfigured authentication class), not a client problem. Failing loud with a 500 surfaces the misconfig to operators; 403 would have leaked "permission" context to the client for a server-only fault. Security-auditor M-1 from the spec-06 review.
- 2026-05-20 — **`materialize_plan` adapter classifies planner events via note-prefix matching, pinned by a contract test.** The structurally correct shape is a `kind: StopEventKind | None` field on `web_api.hos.types.LogEvent`, but that touches `web_api/hos/**` (invariant-#1 surface) and was out of spec-06 scope. The interim contract is `tests/hos/test_note_contract.py` asserting: (a) no prefix is a left-substring of another so the linear scan never silently mis-classifies, (b) every canonical planner note resolves to the expected `StopKind`, (c) named constants ↔ dict-key parity. Code-reviewer C1 from spec-06; promotion tracked in Next Up as `feat/<NN>-planner-event-kind-field`.
- 2026-05-20 — **`GZipMiddleware` lives in MIDDLEWARE between SecurityMiddleware and WhiteNoiseMiddleware.** WhiteNoise gzips static assets only; the JSON plan envelope (~20 KB on a transcontinental trip) was previously shipping uncompressed. ~5-7× compression on the repetitive key set. BREACH attack vector doesn't apply (no secret-containing JSON, no attacker-controlled body). Performance-engineer M1 from spec-06.

- 2026-05-20 — **HOS planner slices long-haul driving legs around constraints in `planner.py::_drive_leg`, deviating from spec 05's single-event-per-leg pseudocode.** Reason: the spec's pseudocode in step 5 emits each pre-leg rule once per leg and then a single driving event for the full leg. That model breaks on a 9h continuous-drive leg starting with 0 drive-since-break: the predicate `apply_break` fires before the leg, but the subsequent 9h drive event still violates §395.3(a)(3)(ii). The goldens (`assessment_break_only`, `assessment_long_haul`, `assessment_cycle_cap`) require the planner to actually split the leg at the constraint boundary — 8h drive → 30-min break → 1h drive, not "30-min break → 9h drive." The `apply_*` predicates in `rules.py` remain the auditable §395 surface (called by `_emit_static_leg` for cycle cap; exercised by `test_rules.py`); the slicer (`_max_drive_chunk` + `_make_constraint_event`) re-derives the same thresholds from the shared constants. Both surfaces track in lockstep via the constants in `rules.py`; end-to-end goldens guard against drift. Documented in `planner.py` and `rules.py` module docstrings.
- 2026-05-20 — **`_smoke.py` is boundary-exempt by filename in `tests/hos/test_boundary.py`.** Reason: spec 05 directs `_smoke.py` to "respect the same stdlib-only rule," but the script must construct a runtime `DirectionsResult` to call `plan_logs(...)` — and `DirectionsResult` is defined in `web_api.integrations.openrouteservice`, which imports Django + requests at module top. Three options to resolve: (a) define a duck-typed stub in `_smoke.py` (adds an abstraction layer for one off-script's benefit); (b) move the smoke outside `web_api/hos/` (violates the spec's directory choice); (c) exempt `_smoke.py` from the boundary test (chosen). The boundary check stays sound because nothing in `web_api.hos` transitively imports `_smoke.py` — running `import web_api.hos` does not pull Django into the test process. The exemption rationale is documented inline in `test_boundary.py` (the only exempt filename is `_smoke.py`; adding more is a senior-review violation by definition).
- 2026-05-20 — **`ALLOWED_TOP_LEVEL` extends spec 05's 9-entry allowlist to 11 entries.** Spec listed `{datetime, dataclasses, enum, decimal, zoneinfo, math, typing, web_api.hos, web_api.integrations.openrouteservice}`. Added: `__future__` (every file uses `from __future__ import annotations` so all annotations are lazy strings) and `collections.abc` (used in `fueling.py` for `Sequence` under TYPE_CHECKING). Both are stdlib and don't weaken the invariant. The full 11-entry allowlist is mirrored verbatim in `architecture.md#Invariants` with the `# senior-review-hook` comment requiring architect-review re-approval for further additions.
- 2026-05-19 — **Spec order reshuffled at spec-03 authoring time.** Reason: the FMCSA-mandated trip-input surface (three addresses + cycle hours + submit) is harder UX work than the HOS planner and unblocks user testing of the form earlier; letting the HOS planner land against the real form (not a hypothetical one) also lowers the chance of planner-to-form mismatch. New order: `03 = trip input + shell` (this spec), `04 = ORS Directions + Trip model upgrade` (replaces the stub), `05 = HOS planner foundation` (formerly spec 03). The HOS planner remains a pure-Python module per architecture invariant #1 when its turn lands.
- 2026-05-19 — **Spec 03 ships as a single FE+BE vertical slice.** Reason: `context/ai-workflow-rules.md#Scoping rules` says split FE/BE into ordered units, but the BE half here is limited to thin Pelias proxy endpoints (`/api/geocode/{autocomplete,search}/`) + a stub `POST /api/trips/` echo — there is no Trip business logic, no HOS, no ORS Directions in this unit. Splitting would force a contract-only intermediate spec with zero user-visible value. The deviation is recorded inline in the spec's Boundary section so senior review sees it intentionally.
- 2026-05-19 — **Custom `NavUser` profile bubble, not Clerk's `<UserButton />`.** Reason: the brand owns its identity surface and the bubble must also host our `ThemeToggle` (`<UserButton />` does not surface custom menu items in Core 3 without unsupported workarounds). Built from shadcn `Avatar` + `DropdownMenu` hydrated by `useUser()` + `useAuth()` from `@clerk/react`. `useAuth().signOut()` powers Sign out. Same primitives + composition will be reused in future shells (account settings, mobile sheet).
- 2026-05-19 — **`sidebar-09` is reference inspiration only; we ship a single icon-collapsible rail.** Reason: shadcn's `sidebar-09` is a dual-nested mail layout (icon rail + content panel of mail items). The trip planner has no list-pane consumer yet (Saved Trips is queued for spec 09), so a second panel would be empty. Single-rail `<Sidebar collapsible="icon">` is the right MVP shape; the layout can extend to a dual-rail later by composing a second `<Sidebar>` without rewriting the first.
- 2026-05-19 — **Address resolution = autocomplete-as-you-type via a server-side Pelias proxy.** Reason: live autocomplete is materially better UX than resolve-on-blur (UI.md §3.1) and the architecture invariant #3 mandates the API key stay server-side. Tradeoff: heavier on the HeiGIT standard-plan geocoding quota (~1000 req/day) — mitigated by a 250 ms client-side debounce + TanStack Query `staleTime: 5 * 60_000` dedupe. Pelias confidence (0–1) + the resolved `lat/lon` are stamped onto the form state at selection time; free-text addresses are rejected by zod at submit-time.
- 2026-05-19 — **Two-spec split for the auth experience.** Reason: `context/ai-workflow-rules.md`'s "one system boundary per unit" rule. Spec 01 lands shared primitives in `packages/ui` (the UI source of truth per `CLAUDE.md`); spec 02 builds the three custom Clerk flows on top inside `apps/web-auth`. Rejected: a single combined spec — would have mixed two boundaries and required two verification paths.
- 2026-05-19 — **Mirror the auth background video with responsive `<source>` tags + poster.** Reason: removes a third-party CDN dependency and gives us a predictable LCP via the poster image (per <https://web.dev/articles/lcp>). Combined desktop + mobile + poster ≤ ~5 MB; encoded with `ffmpeg -movflags +faststart` so streaming starts immediately.
- 2026-05-19 — **Inline email-code verification on `/sign-up`.** Reason: matches Clerk's `email_code` example, avoids a route-change/redirect cycle, and lets focus management move directly to the OTP cell without losing in-progress form state.
- 2026-05-19 — **MFA (TOTP / SMS / backup codes) is out of v1 scope.** Reason: matches `project-overview.md`'s v1 in-scope list (single driver, no admin role). "High-end security" still holds via Clerk Turnstile bot protection, zxcvbn-ts strength gating, and the HaveIBeenPwned (`form_password_pwned`) compromised-password check. Revisit after launch.
- 2026-05-19 — **Clerk instance configuration is owned by the session via the `clerk` CLI, not by the user.** Reason: env vars already hold real instance keys, so the implementer can drive SSO connections, bot protection, password rules, and redirect-URL allowlists directly through the Clerk Backend / Platform API. Eliminates the manual-dashboard handoff and lets the PR body capture the exact instance state via CLI output.
- 2026-05-19 — **End-to-end testing is performed by the session-attached Playwright MCP, not by committed Playwright source.** Reason: E2E coverage must drive a real browser as a real user, but the project does not need (and `architecture.md` does not pin) `@playwright/test` as a repo dependency. The MCP is the runner; artifacts (screenshots, DOM snapshots, trace) are attached to the PR body per scenario.
- 2026-05-19 — **`aria-pressed` (not `role="switch"`) for the show/hide password toggle and the theme toggle.** Reason: the W3C APG button-toggle pattern (<https://www.w3.org/WAI/ARIA/apg/patterns/button/>) prefers `aria-pressed` for two-state buttons without a track/thumb metaphor; the toggle label stays constant so screen readers don't thrash.
- 2026-05-19 — **`<Show when="signed-in">` / `<Show when="signed-out">` as the gating primitive across both apps.** Reason: Clerk Core 3 deprecates `<SignedIn>` / `<SignedOut>` in favor of `<Show>` (<https://clerk.com/docs/guides/development/upgrading/upgrade-guides/core-3>). The architecture file already records this; no implementation has had to pick yet, so the decision lands here.
- 2026-05-19 — **Brand SVGs inlined as JSX inside `BrandMark` and `SpotterLoader`** (spec 01 deviation). Reason: `packages/ui` is a JIT source-consumption package with no bundler (no Vite, tsup, or tsc emit), so the spec's SVGR pipeline is contingent on infrastructure that does not exist. Inline JSX keeps the `packages/ui-only` boundary, lets the loader animation target each circle directly via CSS keyframes scoped by `useId`, and passes the hex-free rule by mapping brand colors to OKLCH ramp CSS variables (`var(--teal-*)`, `var(--red-500)`). `src/assets/brand/` is not created.
- 2026-05-19 — **`packages/ui/package.json#exports` gains one specific key for `./components/theme/use-theme`** (spec 01 deviation). Reason: the spec colocates the `useTheme` hook with the provider, but the existing pattern `./components/*` → `./src/components/*.tsx` only resolves `.tsx`. Node and TypeScript both honor specific keys over pattern keys, so a single targeted override preserves the public API verbatim while keeping the pattern intact for every other component.
- 2026-05-19 — **shadcn `toggle` primitive dropped from the install list** (spec 01 deviation). Reason: the spec's own Step 3 implements `ThemeToggle` as `Button variant="ghost" size="icon"` with `aria-pressed` per the recorded APG decision, so `toggle` would have shipped with no callsite. `code-standards.md` forbids installs without a callsite ("Untouched dependencies are noise").
- 2026-05-19 — **`sonner.tsx` hand-edited to import `useTheme` from our theme primitive** (spec 01 deviation). Reason: shadcn 4's canonical sonner wrapper imports `useTheme` from `next-themes`, which (a) duplicates a theme controller alongside our `ThemeProvider` and (b) yields `theme: string | undefined`, breaking the typecheck under `exactOptionalPropertyTypes`. The minimal 3-line edit replaces the import path and removes the now-obsolete `as ToasterProps["theme"]` cast. The `next-themes` dep is dropped. If we re-run `shadcn add sonner --overwrite` later, this edit must be re-applied.
- 2026-05-19 — **`packages/ui/eslint.config.js` adds an override for `src/components/ui/**`** turning off `import-x/order`, `react-refresh/only-export-components`, `@typescript-eslint/consistent-type-definitions`, `@typescript-eslint/no-unnecessary-condition`, `@typescript-eslint/no-unnecessary-template-expression`, `@typescript-eslint/no-unnecessary-type-conversion`. Reason: those rules fight canonical shadcn output. Per `code-standards.md`, the files under `components/ui/\*` are CLI-owned and re-run-to-update — we adapt the linter rather than patch the generated code.
- 2026-05-19 — **Vitest setup ships an in-memory `Storage` shim and a `matchMedia` stub.** Reason: under vitest 4.1.6 + jsdom 29.1.1 in this pool configuration, `window.localStorage` exists but its prototype methods (`setItem`, `clear`) are not callable, and `window.matchMedia` is not defined. A 30-line shim in `packages/ui/src/test/setup.ts` restores the contract the `ThemeProvider` relies on; production code is unaffected.
- 2026-05-19 — **Apple OAuth dropped from spec 02 scope.** Reason: the Apple Developer account is not provisioned, and the spec's Apple-portal handoff (paste the Clerk-issued return URL into the Services ID config) is impossible without it. Google-only OAuth ships; the spec's Apple-row E2E case is waived. A follow-up `feat/<NN>-apple-oauth` will add the Apple button + Clerk patch + E2E row when the account exists.
- 2026-05-19 — **`@clerk/testing` not installed** (spec 02 open question resolved). Reason: package 2.x peers on `@playwright/test`/`cypress` only — it is a Cypress/Playwright integration helper, not a Vitest mock substitute. The Vitest layer uses hand-rolled `vi.mock("@clerk/react", …)` shims in `apps/web-auth/src/testing/clerk-mocks.ts`. No `@clerk/testing` callsite exists (would violate the no-dependency-without-callsite rule).
- 2026-05-19 — **Per-app env templates renamed `.env.local.example`** (spec 02 cross-cutting). Reason: `.env.local` is the conventional Vite local-overrides filename, so the template should mirror it (`cp .env.local.example .env.local`). The root `.gitignore` allowlists only `!.env.local.example`; the spec-02 commit migrated `apps/web-auth/.env.example` → `.env.local.example`, and `chore/04-remove-legacy-env-examples` migrated `apps/web-app` + `apps/web-api`. `CONTRIBUTING.md` §6 documents the convention going forward; no new `.env.example` may be authored.
- 2026-05-19 — **Wrappers use Clerk Core 3 Future API throughout** (spec 02 corrective fix). Reason: initial wrappers landed against the legacy `SignInResource` / `SignUpResource` (`signIn.create`, `signUp.prepareEmailAddressVerification`, `authenticateWithRedirect`, `setActive`, throws on error). `useSignIn()` / `useSignUp()` in `@clerk/react` v6 actually return `SignInFutureResource` / `SignUpFutureResource`, which expose `signIn.password({ identifier, password })`, `signIn.sso({ strategy, redirectUrl, redirectCallbackUrl })`, `signUp.verifications.{sendEmailCode, verifyEmailCode}`, `signIn.resetPasswordEmailCode.{sendCode, verifyCode, submitPassword}`, `signIn.finalize()`, and resolve to `{ error: ClerkError | null }` rather than throwing. The wrappers + tests + mocks were re-cut to the Future API in commit `0f0bcb4`. `AuthError` is a structural interface (not Clerk's class, which is not exported from `@clerk/shared/types`).
- 2026-05-19 — **Within-auth-feature imports use relative paths** (spec 02 carry-over). Reason: the shared `import-x/no-restricted-paths` Bulletproof zones forbid `@/features/*` imports inside any `features/` file. `apps/web-auth/eslint.config.js` adds an `except: ["./auth"]` carve-out scoped to `src/features/auth/**` so within-feature siblings can import each other via relative paths; cross-feature imports still fire the original rule when a second feature lands.
- 2026-05-19 — **`apps/web-app/eslint.config.js` mirrors the spec-02 Bulletproof carve-out** for `src/features/trip-planner/**`. Reason: same as the auth carve-out — the package-level rule treats every `src/features/*` file as unable to import from `src/features`, which is correct for cross-feature but blocks intra-feature `@/features/trip-planner/*` aliases. The carve-out scopes the relaxation to one feature; cross-feature imports remain forbidden.
- 2026-05-19 — **`OPENROUTESERVICE_API_KEY` kept, bumped to `SecretStr`** (spec 03 vs. spec text). Reason: the spec text used `ORS_API_KEY`, but the project README + Fly.io secrets already use the long name. Renaming would rotate prod secrets; the longer name is also more grep-able. Bump to `SecretStr` (security hygiene — `repr()` masks the value, the client calls `.get_secret_value()` once at the boundary). The spec file was corrected inline post-implementation.
- 2026-05-19 — **`web_api/settings/test.py` runs Pytest against SQLite in-memory.** Reason: CI doesn't provision Postgres; the existing pytest config (`testpaths = ["tests"]`, top-level layout) doesn't need a DB except for the new Trip ORM tests. SQLite handles `UUIDField` / `DecimalField` / the `(user_id, -created_at)` Index correctly via Django's built-in adapters. The test settings extend `base.py` (not `dev.py`, which has the `AllowAny` override); a stand-in `CLERK_SECRET_KEY = "sk_test_unit"` lets `ClerkAuthentication.authenticate` reach the patched `authenticate_request` SDK function. `prod.py` does not import the override.
- 2026-05-19 — **`dev.py` clears `DATABASES["default"]["OPTIONS"]`** (psycopg 3 incompat). Reason: `OPTIONS={"pool": True}` is incompatible with Django's `CONN_MAX_AGE` (psycopg refuses pooled connections alongside Django's connection reuse — raises `ImproperlyConfigured("Pooling doesn't support persistent connections.")` on first DB access). Local dev is single-process `runserver`, no pooling benefit, so we drop the pool in dev. Prod keeps `pool=True` via base settings — `base.py` is unchanged so the gunicorn workers still pool.
- 2026-05-19 — **`exactOptionalPropertyTypes: false` in `packages/ui/tsconfig.json` + `apps/web-app/tsconfig.app.json`.** Reason: shadcn-generated `dropdown-menu.tsx` and `slider.tsx` spread `{...props}` containing `checked: CheckedState | undefined` / `value: number[] | undefined` into Radix primitives that don't accept explicit `undefined` under the strict flag. Per the spec-01 precedent for ESLint ("adapt the linter, not the generated code"), we relax the TS flag for the packages that source-consume shadcn primitives. The shared `@outbound/typescript-config/base.json` keeps the flag on — only the two consumers opt out.
- 2026-05-19 — **`BrandMark` gains a `variant="icon"`** (spec 03 / round-1 code review). Reason: the existing `mark-only` variant only swapped the palette to `currentColor` while keeping the full `385×128` wordmark — useless for an icon-rail header where the available width is `3rem`. The new variant clips the viewBox to `0 21 74 70`, renders only the leading "O" mark + the inner red circle, and uses the FULL palette so the brand red shows. Replaces the local `BrandMarkIcon` duplicate that lived in `app-sidebar.tsx`.
- 2026-05-19 — **`TripRetrieveView` returns 404 (not 403) for foreign trips.** Reason: distinct 403/404 leaks whether the UUID exists for another user. UUIDv4's 122-bit entropy makes brute-force impractical, but the principle matters once we lift to integer ids or sequential identifiers. Single `Trip.objects.filter(pk=id, user_id=request.user_id).first()` query — same plan, no oracle.
- 2026-05-19 — **`requests.Session` + `Retry` policy in `web_api/integrations/openrouteservice.py`.** Reason: TLS reuse saves ~50–150 ms per call (load-bearing for spec 04's Directions traffic). The `Retry(total=1, backoff_factor=0.3, status_forcelist=(502, 503, 504))` adds one retry on transient 5xx; **429 is explicitly NOT retried** so `OrsRateLimitError` fires deterministically and the FE doesn't burn three attempts on the same exhausted quota window.
- 2026-05-19 — **`OPENROUTESERVICE_BASE_URL` validator allowlist** (security M2). Reason: SSRF defense. The base URL is settings-driven (good — invariant #3 still holds), but with no validation an operator misconfiguring the env could ship the API key to an arbitrary host. The pydantic-settings `@field_validator` restricts the value to `{"https://api.openrouteservice.org"}`. Self-hosting the ORS engine later requires explicitly extending the allowlist.
- 2026-05-19 — **TanStack `retry: false` on the geocoder + trips-detail queries.** Reason: 4xx + 429 are deterministic — retrying doesn't help and the geocoder retry burns 3× the HeiGIT 1k/day quota per 429. Server-side caching (queued for spec 04) will further protect the quota.
- 2026-05-19 — **`packages/ui/src/components/ui/slider.tsx` patched** (spec 03 a11y blocker). Reason: shadcn's canonical Slider (a) renders a 16×16 thumb, failing WCAG 2.5.8 Target Size (Minimum), and (b) doesn't propagate `aria-label`/`aria-labelledby`/`aria-describedby` to the inner `<SliderPrimitive.Thumb>` (the element that carries `role="slider"` — what screen readers announce). The patch bumps the thumb to `size-6` (24×24 px) and forwards the three aria attributes. Mirrors the spec-01 `sonner.tsx` precedent: re-running `shadcn add slider --overwrite` requires re-applying the patch.
- 2026-05-19 — **`apps/web-app/src/app/provider.tsx` wraps the tree in `<ThemeProvider>`** (spec 03 bug fix, browser-smoke catch). Reason: `NavUser`'s dropdown hosts `<ThemeToggle>`, which reaches for `useTheme` from `@outbound/ui/components/theme/use-theme`. Without an enclosing `ThemeProvider` the hook throws `useTheme must be used within a ThemeProvider` at runtime (caught by the React Router default ErrorBoundary). The provider tree now mirrors `apps/web-auth/src/app/provider.tsx`. The `<Suspense>` fallback is also bumped to `<SpotterLoader size="lg">` so deferred routes get the brand loader.
- 2026-05-19 — **Local Postgres role + DB created for the dev workflow.** Reason: the README documents `DATABASE_URL=postgresql://outbound:outbound@localhost:5432/outbound_dev` but the user's local Postgres didn't yet have the `outbound` role or `outbound_dev` database. Created via `psql -h localhost -d postgres -U mateo -c "CREATE ROLE outbound WITH LOGIN PASSWORD 'outbound' CREATEDB; CREATE DATABASE outbound_dev OWNER outbound;"`. Matches the README; reversible (`DROP DATABASE outbound_dev; DROP ROLE outbound;`).
- 2026-05-19 — **`SidebarProvider` already ships the `Cmd/Ctrl+B` keybinding.** Reason: spec 03 §Step 3 called for a custom `sidebar-trigger.tsx` wrapper that adds a global keydown listener; on inspection of the installed `sidebar.tsx` (lines 99-106), `SidebarProvider` already attaches a `window.addEventListener("keydown", …)` for `SIDEBAR_KEYBOARD_SHORTCUT = "b"` with `metaKey` / `ctrlKey`. No wrapper needed — the spec text was wrong about a missing keybinding. Verified live in browser smoke.
- 2026-05-19 — **`use-current-location.tsx` deferred from spec 03.** Reason: the spec called for a button that reverse-geocodes `navigator.geolocation.getCurrentPosition` via `/api/geocode/search/?text=<lat,lon>`, but Pelias `/search` is text-based — proper reverse needs `/geocode/reverse?point.lat=…&point.lon=…`. Adding a `/api/geocode/reverse/` endpoint + `geocode_reverse(lat, lon)` client method is small but introduces a new BE surface not justified by the verification checklist (which doesn't mention geolocation). Tracked as a follow-up (`feat/<NN>-current-location`); the form is still usable without it (driver types their location).

## Session Notes

- The repo is fully scaffolded as of this session: `apps/web-app`, `apps/web-auth`, `apps/web-api`, `packages/ui`, `packages/eslint-config`, `packages/typescript-config` all present with locked versions (`@clerk/react` 6.6.6, React 19.2.6, Tailwind 4.3.0, Vite 8.0.13, TS 6.0.3, Django 5.2, `clerk-backend-api` 5.0.6). The spec-02 unit added: `@clerk/shared` 4.12.2, `react-hook-form` 7.76.0, `@hookform/resolvers` 5.2.2, `@zxcvbn-ts/core` 3.0.4, `@zxcvbn-ts/language-common` 3.0.4, `@zxcvbn-ts/language-en` 3.0.2 to `apps/web-auth`; `field`, `input-group`, `textarea` shadcn primitives to `packages/ui`.
- `packages/ui/src/styles/globals.css` already ships the OKLCH teal + red ramps, the `@theme inline` block, and the Geologica / DM Sans / JetBrains Mono fallbacks per `context/ui-context.md`. Spec 01 deliberately does not edit it; spec 02 does not edit it either.
- `apps/web-app` and its `<ClerkProvider>` (with `signInUrl` / `signUpUrl` → `VITE_AUTH_*`) are untouched in both specs.
- Clerk dev instance `arriving-bobcat-41` (app `app_3Dx34R192u5uxbsEivdrMcs7ff0`, instance `ins_3Dx34SzmpNQWiZU09qgydJuzkJd`) is fully configured by the spec-02 CLI session — see the PR body "Clerk instance configuration" section for the before/after JSON snapshots. Working configuration: Google OAuth enabled, sign-up Turnstile (smart) enabled, password rules `{min_length: 10, require_lowercase, require_uppercase, require_numbers, min_zxcvbn_strength: 3, disable_hibp: false}`, email verification strategy `["email_code"]`, `verify_at_sign_up: true`.
- The session-attached Playwright MCP was registered (per `claude mcp list` shows `playwright: npx @playwright/mcp@latest - ✓ Connected`) but its tools were not exposed in the spec-02 Claude Code session's tool catalog, so the §10 E2E matrix could not run in-session. The dev-server smoke check (build green, routes 200, video assets 200) is captured in the PR body. The full E2E matrix is tracked as `feat/<NN>-auth-e2e-matrix` under Next Up.
- Husky hooks (`pre-commit` lint-staged, `pre-push` typecheck + test + push protection on `main` / `develop`, `commit-msg` commitlint) and the single CI workflow (`.github/workflows/ci.yml`) remain wired; spec-02 commits exercise them on every commit.
- Resume cue for the next session: author `context/specs/03-hos-planner-foundation.md` and start `apps/web-api/web_api/hos/` (pure Python, no Django imports, golden tests against `docs/assets/example-complete-grid.png` and the FMCSA paragraphs in `docs/interstate-truck-driver-guide.md`).
- 2026-05-20 — Spec 03 shipped on `feat/03-trip-input-foundation`. Five sub-agent reviews (code-reviewer / architect-review / security-auditor / ui-visual-validator / performance-engineer) ran against the diff; round-1 fixes landed in commit `60b324b` (a11y, security, perf). Resume cue for the next session: author `context/specs/04-ors-directions-trip-pipeline.md` — extend `web_api.integrations.openrouteservice` with `directions_hgv`, persist polyline/segments/summary on `Trip` (or a sibling table), add server-side ORS cache (`trip_route_cache` keyed by `sha256(coords|profile)`) + DRF `UserRateThrottle` on the geocoder (the two security/perf deferrals from spec-03 review). Local Postgres bootstrap done: role `outbound` + DB `outbound_dev` exist on `localhost:5432`.
- 2026-05-20 — **Spec 04 amended post-live-smoke** (senior-review directive). The original design persisted a FAILED Trip row on ORS rejection + pushed the user to `/trips/:id` to see the inline error — bad UX (back-and-forth navigation, half-resolved records). Amendment: `TripCreateView` now validates via ORS BEFORE persisting any row; routing failures propagate as HTTP envelopes (400/429/502), the FE toasts `ApiError.body.detail`, and the form state is preserved so the user can retry without navigating. Mechanical consequences (a row exists ⇔ planned): `TripStatus` enum removed, `route_error_code` column removed, FE discriminated union collapsed to a flat shape, `RouteSummary` drops the status switch (renders only planned), `TripDetailPanel` drops the badge. Migration 0003 deletes orphan rows (`route_polyline IS NULL`) before dropping the columns. Test counts: 69 BE → 69 BE (unchanged; FAILED-marking tests replaced with raises-OrsError tests + view-level HTTP-mapping tests), 43 FE → 35 FE (status-branch tests removed; pre-validation flow has fewer branches by design). Spec 04 amendments documented inline in `context/specs/04-ors-directions-trip-pipeline.md#Decisions amended post-live-smoke`.
- 2026-05-20 — Spec 04 shipped on `feat/04-ors-directions-trip-pipeline` (9 commits + 1 docs commit on the spec text). Architect-review fired twice per the spec workflow: first against the spec text (caught the migration-ordering issue + the missing 401 branch → fixed in `62de3f4`), then against the diff (all 10 invariant checks pass). Five round-2 diff reviews: code-reviewer (ship it), architect-review (architecture clean), security-auditor (security clean, no CRITICAL/MAJOR), ui-visual-validator (blocked on weak focus indicator on the "Plan a new trip" link → fixed in `8cb241a`), performance-engineer (+11 KB raw / +3 KB gzip JS, zero leaflet leakage, perf clean). The full Playwright MCP browser walk (Step 8) was NOT run autonomously this session — it requires the user's local Clerk + ORS keys in `apps/web-api/.env.local`; instead a Django `manage.py check` boot smoke and a fresh Vite production build smoke (no leaflet chunk) replaced it. The user should still walk the golden trip (Richmond → Fredericksburg → Newark, 35.0 cycle hours) in a browser before merging, plus verify the cache-hit path on a 2nd submit and the 429 throttle path on rapid POSTs. **Resume cue for the next session**: author `context/specs/05-hos-planner-foundation.md` and start `apps/web-api/web_api/hos/` — pure-Python module, no Django imports (invariant #1), `LogEvent` dataclasses, golden tests against `docs/assets/example-complete-grid.png` and the §395.8/§395.3 paragraphs in `docs/interstate-truck-driver-guide.md`. The trip pipeline is ready to feed it: `plan_trip` already passes `(current, pickup, dropoff)` coordinates + the route polyline through; spec 05 adds the HOS planner that consumes a `DirectionsResult` and emits a `list[LogEvent]`. Cleanup follow-ups queued under Next Up: `chore/<NN>-redis-throttle-cache` (Redis-back the DRF throttle cache before multi-worker production deployment), `chore/<NN>-trip-response-schema-trim` (narrow the public exports of `trip-response.ts`).

- 2026-05-20 — Spec 05 shipped on `feat/05-hos-planner-foundation` (5 commits: 1 `feat(hos)` for the module, 2 `test(hos)` for boundary+units and goldens, 1 `docs(hos)` for README+smoke, 1 `docs(context)` for architecture.md mirror + tracker). Four sub-agent reviews ran against the diff (architect-review / code-reviewer / python-pro / test-automator). Findings addressed inline: (a) test-automator MAJOR 5 — `test_cycle_cap_subsumes_window` now asserts restart event content (duration + note) to close the silent-pass gap; (b) python-pro MAJOR 2 — `_emit_static_leg` docstring now says the 14h window enforcement is lazy (handled on next driving leg), not "does not apply"; (c) code-reviewer MAJOR 1+2 — `planner.py` and `rules.py` module docstrings now honestly describe the dual surface (rules.py predicates are the auditable §395 single-source-of-truth; `_max_drive_chunk` re-derives the same thresholds from the shared constants — both layers track in lockstep); (d) python-pro MAJOR 1 — added inline comment in `_max_drive_chunk` documenting the Decimal-int truncation safety under the integer-minute duration contract; (e) test-automator MAJOR 1 — added `test_off_duty_window_fires_when_window_already_expired` boundary case. Deferred (MINOR, non-blocking): per-test §395 citations in docstrings, AAA blank lines, dead-field cleanup (struct fields used by spec 06's adapter), `factory-boy`-style fixtures (not needed), DST-transition golden (explicitly out of v1). The CLI smoke (`uv run python -m web_api.hos._smoke`) was walked against the John Doe narrative in `docs/interstate-truck-driver-guide.md:207-222`; all four scenarios (simple/break-only/long-haul/cycle-cap) produce §395-correct event sequences. Browser/UX validation is deferred to spec 06 (the planner has no FE surface in this unit). **Resume cue for the next session**: author + implement `context/specs/06-hos-persistence-start-at-plan-endpoint.md` — wire `plan_logs` into `services.plan_trip` via a thin adapter, add three Django models (`TripStop`, `LogEvent`, `LogDay`), add `start_at` field on `Trip` + datetime picker on the form, ship `GET /api/trips/<uuid:id>/plan/`. That spec unblocks the browser walk for the assessment trip.
