We use gitflow, each spec gets executed into its own branch. 

Each change lives on its own branch we follow the development cycle of an enterprise team.

All code gets to development through PRs that trigger code reviews. 