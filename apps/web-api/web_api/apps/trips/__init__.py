"""Trips feature — stub Trip model + create/retrieve endpoints.

Spec 03 ships only the input echo: the driver's three addresses + cycle hours
land in Postgres and come back out. Spec 04 introduces the ORS Directions
pipeline and upgrades ``Trip`` with the route / polyline / segments fields.
"""
