"""Container package for feature Django apps.

Each feature lands as a sub-package here (e.g. `web_api.apps.trips`,
`web_api.apps.logs`) with its own `models.py`, `views.py`, `serializers.py`,
`urls.py`, `migrations/`. Feature URL configs are included in
`web_api/urls.py` once they ship.
"""
